# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.eggs/
build/
dist/
pip-wheel-metadata/
share/python-wheels/

# Virtual environments
.venv/
venv/
env/
ENV/

# Test & type-checker caches
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Audit logs (runtime artifacts at the repo root — sensitive, never commit).
# Anchored with a leading slash so this pattern only matches the top-level
# ./audit/ directory created by the running service. Without the anchor,
# `audit/` would also match `service/audit/` (the source subpackage) and
# silently strip those files from git checkouts — which would break CI.
/audit/
*.jsonl
!service/tests/**/*.jsonl

# Sample EEG payloads under samples/ — local scratch only.
# Demo EDFs that ship with the repo live under docs/examples/ and are NOT
# gitignored (they're educational fixtures, consistent with how PhysioNet
# / OpenNeuro / BCI Competition share short EEG recordings).
samples/*
!samples/README.md

# QVAC bridge plugin build artifacts (Phase 2)
plugin-manifest/node_modules/
plugin-manifest/dist/

# OS / environment
.env
.env.local
*.local

# Logs
*.log
logs/

# Internal docs — not for public repo.
# Keep on disk locally; never push.
docs/Cognitive Edge - CHAIN_ARCHITECTURE.md
docs/Cognitive Edge - PRODUCT_SPEC.md
docs/SkyBrain Analyze - README.md
docs/SkyBrain BCI Studio - TETHER_APPLICATION_BRIEF.md
docs/SkyBrain INFO.md

# Grant application artifacts (local-only; PDFs are derived from docs/*.md)
application/
